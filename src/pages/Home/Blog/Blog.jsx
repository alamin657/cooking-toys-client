import React from 'react';

const Blog = () => {
    return (
        <div>
            <div>
                <h2>What is an access token and refresh token? How do they work and where should we store them on the client-side?</h2>
                <p>Access Token:Access token is a credential that is issued to a client (such as a web or mobile application) by an authentication server after a successful authentication process.</p>
                <p>Refresh Token:A refresh token is a long-lived credential that is also issued by the authentication server during the authentication process. </p>
            </div>
        </div>
    );
};

export default Blog;